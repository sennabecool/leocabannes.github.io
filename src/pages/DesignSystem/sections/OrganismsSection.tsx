import styles from './sections.module.css'

export default function OrganismsSection() {
  return (
    <section id="organisms" className={styles.section}>
      <h2 className={styles.sectionTitle}>Organisms</h2>
      <p className={styles.placeholder}>
        Organism components will appear here as they are built from Figma designs.
      </p>
    </section>
  )
}
